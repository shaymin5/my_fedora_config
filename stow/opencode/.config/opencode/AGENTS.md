# important rules

- 关于python相关内容，一律用uv管理环境。
- 禁止使用系统的`python`, `python3`, `pip`, `pip3`命令，只允许使用`uv`来执行python命令和运行python代码，使用`uv add`, `uv remove`等命令 instead of with `pip`。
- 使用`basedpyright`来检查python代码，使用`pytest`来运行测试。

# 交互rule

与用户交谈用中文
